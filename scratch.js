const fs = require('fs');

const rawData = {
  "batch_1_fundamental_weaves": {
    "1_plain_weave": { ... }, // I will put the entire user JSON here
  }
};
// But I don't need a Node script. I can just build the TS file directly.
